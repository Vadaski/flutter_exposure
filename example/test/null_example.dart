import 'package:flutter/material.dart';

int sum(int a, int b) {
  return a + b;
}

void main() {
  int a;
  int b = 1;
  // sum(a, b);
}

// 成员变量不能为不赋值
class A {
  int x;
  A({this.x});
}

// 方法内空检查

// 方法 访问了一个外部的值

// late Key word

// !operator

//
